- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.dotfiles_cq: ''
    ~/.bashrc:
    ~/.bash:

- shell:
    - [git submodule update --init --recursive, Installing submodules]
