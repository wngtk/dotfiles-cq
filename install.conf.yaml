- defaults:
    link:
      create: true
      relink: true
      force: true

- clean: ['~', '~/.config']

- link:
    ~/.dotfiles_local: ''
    ~/.config/nvim/lua/local-settings: nvim/lua/local-settings
    ~/.config/fish/config_local_after.fish:

- shell:
    - [git submodule update --init --recursive, Installing submodules]
