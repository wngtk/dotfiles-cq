- defaults:
    link:
      create: true
      relink: true
      force: true

- clean: ['~', '~/.config']

- link:
    ~/.dotfiles_cq: ''

- shell:
    - [git submodule update --init --recursive, Installing submodules]
