- defaults:
    link:
      create: true
      relink: true
      force: true

- clean: ['~', '~/.config']

- link:
    ~/.dotfiles_local: ''
    ~/.config/nvim/lua/local: nvim/lua/local
    ~/.config/fish/config_local_after.fish:

- shell:
    - [git submodule update --init --recursive, Installing submodules]
